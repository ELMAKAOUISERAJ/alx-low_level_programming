#include <stdio.h>
/**
 * main - prints its name followed by new line
 * @argc: n of parameters 
 * @argv: array of parameters
 * Return: alwaus 0 succes
 */
int main(int argc, char const *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
